# Environment Configuration Example
# Copy this file to .env and update with your actual values

# ============================================
# Database Configuration
# ============================================
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/agent_squad_dev
REDIS_URL=redis://redis:6379

# ============================================
# Application Settings
# ============================================
SECRET_KEY=dev-secret-key-change-in-production
ENV=development
DEBUG=true
HOST=0.0.0.0
PORT=8000
PYTHONPATH=/workspace
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# ============================================
# Jira Configuration (Local Docker)
# ============================================
# For local Docker setup, use basic auth with admin credentials
JIRA_URL=http://localhost:8080
JIRA_USERNAME=admin@example.com
JIRA_API_TOKEN=admin123

# For Jira Cloud (production), use:
# JIRA_URL=https://your-company.atlassian.net
# JIRA_USERNAME=your-email@company.com
# JIRA_API_TOKEN=your-actual-api-token

# ============================================
# Confluence Configuration (Local Docker)
# ============================================
# For local Docker setup, use basic auth with admin credentials
CONFLUENCE_URL=http://localhost:8090
CONFLUENCE_USERNAME=admin@example.com
CONFLUENCE_API_TOKEN=admin123

# For Confluence Cloud (production), use:
# CONFLUENCE_URL=https://your-company.atlassian.net/wiki
# CONFLUENCE_USERNAME=your-email@company.com
# CONFLUENCE_API_TOKEN=your-actual-api-token

# ============================================
# GitHub Configuration
# ============================================
# Get token from: https://github.com/settings/tokens
GITHUB_TOKEN=your-github-personal-access-token

# ============================================
# Git Configuration
# ============================================
# Local repo path for git operations
GIT_REPO_PATH=/workspace/repo

# ============================================
# MCP Configuration
# ============================================
# MCP servers are auto-configured via integrations
# No additional configuration needed

# ============================================
# LLM Configuration (if needed)
# ============================================
OPENAI_API_KEY=your-openai-api-key
ANTHROPIC_API_KEY=your-anthropic-api-key

# ============================================
# Stripe Configuration (for billing)
# ============================================
STRIPE_SECRET_KEY=your-stripe-secret-key
STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key
STRIPE_WEBHOOK_SECRET=your-stripe-webhook-secret

# ============================================
# Email Configuration (optional)
# ============================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# ============================================
# Agent Configuration
# ============================================
# Agents are powered by the Agno framework (enterprise-grade)
# with persistent sessions, memory, and production-ready architecture
# Note: This variable is deprecated (Agno is now the only implementation)
USE_AGNO_AGENTS=true

# ============================================
# Message Bus Configuration
# ============================================
# Options: "memory" (single server, dev/test) or "nats" (production, distributed)
# Default: nats (production-ready with persistence and horizontal scaling)
MESSAGE_BUS=nats

# NATS Configuration (required for MESSAGE_BUS=nats)
# NATS provides distributed messaging with persistence and horizontal scaling
NATS_URL=nats://localhost:4222
NATS_STREAM_NAME=agent-messages
NATS_MAX_MSGS=1000000
NATS_MAX_AGE_DAYS=7
NATS_CONSUMER_NAME=agent-processor

# ============================================
# Frontend Configuration
# ============================================
NEXT_PUBLIC_API_URL=http://localhost:8000
NODE_ENV=development
